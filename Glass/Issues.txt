GlassVideo:

Glass:
No way to delete a profile
Need to be able to edit characters
Changes to relay groups needs to be tested (is custom control wired?)
Glass is not capturing network traffic.
pcap has patch level hard-coded - embed patch level and server type in pcapng metadata with libpcap
need to finish Inference in preparation for inevitable patch
consider adding mapping, using Brewall maps and mob data.  The Brewall maps are always loaded in the EQ folder.  
I'm not sure how expensive it is to render the maps.
Need to identify the current zone for characters.  (Chat server packet gets this)
Mob repository needs to be created.
Connection still has multiple streams that are not used.
Field data editor in Inference does not have modify support for optional groups. See "Fields".
Inference has LoadPatchOpcodes which duplicates the PatchRegistry
PacketEnvelope should include a Connection and not the ip addresses and ports.
SessionRegistry is not checking for an existing Connection properly

ISXGlass:

Phase 3 (multi-machine operations):

When Glass connects to a remote ISXGlass instance, it knows that machine's hostname from the config file
Look up or insert that hostname in Machines, get its id
Layouts and monitors for that machine are stored under its id
Client file generation uses the monitor dimensions for the target machine, not necessarily the current machine

Cross-machine layout loading behaviour